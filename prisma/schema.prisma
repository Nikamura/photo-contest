generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Contest {
  id   String @id @default(uuid())
  name String
}

model ContestItem {
  id   String @id @default(uuid())
  file String
}
